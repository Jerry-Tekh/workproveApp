/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 15: Turbopack is available but optional via --turbopack flag
  // No webpack config needed for this project
};

module.exports = nextConfig;
